const loader = document.querySelector(".loader");

// window.addEventListener('load', () => {
//     x = loader.classList.add('fondu-out');
// })

function LoadPage() { 
  loader.classList.add("fondu-out")
  };

setTimeout(LoadPage, 5000); 
